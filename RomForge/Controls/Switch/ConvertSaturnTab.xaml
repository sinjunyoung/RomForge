<UserControl x:Class="RomForge.Controls.Switch.ConvertSaturnTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:core="clr-namespace:NSW.WPF.UI;assembly=NSW.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="580" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="205"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="5,5,5,0">
            <Grid Height="36">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Margin="0,0,5,5" Background="#12FFFFFF" BorderBrush="#30FFFFFF" BorderThickness="1" CornerRadius="6" AllowDrop="True" PreviewDrop="Cue_Drop" PreviewDragOver="OnPreviewDragOver">
                    <Grid>
                        <TextBox Text="{Binding CuePath, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" Foreground="#DCDCDC" FontSize="11" VerticalContentAlignment="Center" Padding="10,0" IsEnabled="{Binding IsIdle}"/>
                        <TextBlock Text="CUE 파일을 드래그&amp;드롭하세요" Visibility="{Binding CueHintVisibility}" Foreground="#50FFFFFF" FontSize="11" VerticalAlignment="Center" Margin="15,0,0,0" IsHitTestVisible="False"/>
                    </Grid>
                </Border>
                <Button Grid.Column="1" Margin="0,0,5,5" Width="30" Height="30" Padding="0" BorderThickness="1" Style="{StaticResource GrayButton}" Command="{Binding BrowseCueCommand}" IsEnabled="{Binding IsIdle}">
                    <Image Source="/Assets/Images/AddFile.png" Width="30" Height="30" RenderOptions.BitmapScalingMode="HighQuality"/>
                </Button>
            </Grid>
            <Grid Height="36">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Margin="0,0,5,5" Background="#12FFFFFF" BorderBrush="#30FFFFFF" BorderThickness="1" CornerRadius="6" AllowDrop="True" PreviewDrop="Nsp_Drop" PreviewDragOver="OnPreviewDragOver">
                    <Grid>
                        <TextBox Text="{Binding NspPath, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" Foreground="#DCDCDC" FontSize="11" VerticalContentAlignment="Center" Padding="10,0" IsEnabled="{Binding IsIdle}" />
                        <TextBlock Text="원본 NSP/NSZ 파일을 드래그&amp;드롭하세요" Visibility="{Binding NspHintVisibility}" Foreground="#50FFFFFF" FontSize="11" VerticalAlignment="Center" Margin="15,0,0,0" IsHitTestVisible="False"/>
                    </Grid>
                </Border>
                <Button Grid.Column="1" Margin="0,0,5,5" Width="30" Height="30" Padding="0" BorderThickness="1" Style="{StaticResource GrayButton}" Command="{Binding BrowseNspCommand}" IsEnabled="{Binding IsIdle}">
                    <Image Source="/Assets/Images/AddFile.png" Width="30" Height="30" RenderOptions.BitmapScalingMode="HighQuality"/>
                </Button>
            </Grid>
            <Grid Height="36">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Margin="0,0,5,5" Background="#12FFFFFF" BorderBrush="#30FFFFFF" BorderThickness="1" CornerRadius="6" AllowDrop="True" PreviewDrop="Nsp_Drop" PreviewDragOver="OnPreviewDragOver">
                    <Grid>
                        <TextBox Text="{Binding WorkPath, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" Foreground="#DCDCDC" FontSize="11" VerticalContentAlignment="Center" Padding="10,0" IsEnabled="{Binding IsIdle}" />
                        <TextBlock Text="작업 폴더를 선택하세요" Visibility="{Binding WorkHintVisibility}" Foreground="#50FFFFFF" FontSize="11" VerticalAlignment="Center" Margin="15,0,0,0" IsHitTestVisible="False"/>
                    </Grid>
                </Border>
                <Button Grid.Column="1" Margin="0,0,5,5" Width="30" Height="30" Padding="0" BorderThickness="1" Style="{StaticResource GrayButton}" Command="{Binding BrowseWorkCommand}" IsEnabled="{Binding IsIdle}">
                    <Image Source="/Assets/Images/AddFolder.png" Width="30" Height="30" RenderOptions.BitmapScalingMode="HighQuality"/>
                </Button>
            </Grid>

        </StackPanel>

        <Grid Grid.Row="1" Margin="5,0,5,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#12FFFFFF" BorderBrush="#30FFFFFF" BorderThickness="1" CornerRadius="8" AllowDrop="True" PreviewDrop="Cover_Drop" PreviewDragOver="OnPreviewDragOver" IsEnabled="{Binding IsIdle}">
                <StackPanel VerticalAlignment="Center">
                    <Image Source="/Assets/Images/Drag.png" Width="28" Height="28" Margin="0,0,0,8" Visibility="{Binding CoverHintVisibility}"/>
                    <TextBlock Text="이미지 파일을" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#50FFFFFF" FontSize="11" IsHitTestVisible="False" Visibility="{Binding CoverHintVisibility}"/>
                    <TextBlock Text="드래그&amp;드롭 하세요" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#50FFFFFF" FontSize="11" IsHitTestVisible="False" Visibility="{Binding CoverHintVisibility}"/>
                    <Image x:Name="imgCover" Source="{Binding CoverImageSource}" Stretch="Uniform" Margin="5" MouseLeftButtonDown="ImgCover_MouseLeftButtonDown"/>
                </StackPanel>
            </Border>

            <StackPanel Grid.Column="1" Margin="15,0,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,5,0">
                        <TextBlock Text="게임명" FontSize="12" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding GameTitle, UpdateSourceTrigger=PropertyChanged}" Background="#12FFFFFF" BorderBrush="#30FFFFFF" BorderThickness="1" Foreground="#DCDCDC" FontSize="11" Height="36" VerticalContentAlignment="Center" Padding="10,0" IsEnabled="{Binding IsIdle}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="5,0,5,5">
                        <TextBlock Text="배급사" FontSize="12" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding Publisher, UpdateSourceTrigger=PropertyChanged}" Background="#12FFFFFF" BorderBrush="#30FFFFFF" BorderThickness="1" Foreground="#DCDCDC" FontSize="11" Height="36" VerticalContentAlignment="Center" Padding="10,0" IsEnabled="{Binding IsIdle}"/>
                    </StackPanel>
                </Grid>
                <Grid Margin="0,5,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,5,0">
                        <TextBlock Text="게임 ID" FontSize="12" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding GameId}" IsReadOnly="True" Background="#12FFFFFF" BorderBrush="#30FFFFFF" BorderThickness="1" Foreground="#DCDCDC" FontSize="11" Height="36" VerticalContentAlignment="Center" Padding="10,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="5,0,0,0">
                        <TextBlock Text="버전" FontSize="12" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding GameVersion}" IsReadOnly="True" Background="#12FFFFFF" BorderBrush="#30FFFFFF" BorderThickness="1" Foreground="#DCDCDC" FontSize="11" Height="36" VerticalContentAlignment="Center" Padding="10,0"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" Background="#12FFFFFF" BorderBrush="#30FFFFFF" BorderThickness="1" CornerRadius="8" Padding="12,8" Margin="5,0,5,5" IsEnabled="{Binding IsIdle}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="설정" Foreground="#D4AF37" FontSize="12" FontWeight="Bold" Margin="0,0,15,0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <CheckBox Content="와이드 스크린" IsChecked="{Binding WideScreen}" Foreground="#DDDDDD" FontSize="11" VerticalAlignment="Center" Margin="5,0,15,0"/>

                <TextBlock Text="사운드 볼륨:" Foreground="#DDDDDD" FontSize="11" VerticalAlignment="Center" Margin="5,0,5,0"/>
                <ComboBox Width="90" Height="26" ItemsSource="{Binding SoundVolumeItems}" SelectedItem="{Binding SelectedSoundVolume}" VerticalContentAlignment="Center" FontSize="11">
                    <ComboBox.ItemContainerStyle>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="Foreground" Value="White"/>
                        </Style>
                    </ComboBox.ItemContainerStyle>
                </ComboBox>
            </StackPanel>
        </Border>

        <Grid Grid.Row="3" Margin="5,0,5,5">
            <core:IconButton DefaultText="작업 시작" DefaultIcon="/Assets/Images/Run.png" CancelText="취소" CancelIcon="/Assets/Images/Cancel.png" IsRunning="{Binding IsConverting}" Click="BtnStart_Click"/>
        </Grid>

        <Border Grid.Row="4" Background="#12FFFFFF" BorderBrush="#30FFFFFF" BorderThickness="0,1,0,0" CornerRadius="8" Margin="5,0,5,5" Padding="8,0">
            <Grid x:Name="progressArea" Height="28">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="140"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" x:Name="progressLabel" Text="{Binding ProgressLabel}" Foreground="#A0A0A0" FontSize="11" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                <TextBlock Grid.Column="1" x:Name="progressPercent" Margin="0,0,10,0" Text="{Binding ProgressPercent}" Foreground="#4CAF50" FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="2" x:Name="progressSpeed" Margin="0,0,10,0" Text="{Binding ProgressSpeed}" Foreground="Gray" FontSize="11" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="3" x:Name="progressTime" Margin="0,0,10,0" Text="{Binding ProgressTime}" Foreground="#4CAF50" FontSize="11" VerticalAlignment="Center"/>
                <Grid Grid.Column="4" Height="10" VerticalAlignment="Center">
                    <Border Background="#25FFFFFF" CornerRadius="5"/>
                    <Border x:Name="barTrack" CornerRadius="5" ClipToBounds="True">
                        <Border Background="#4CAF50" CornerRadius="5" HorizontalAlignment="Left">
                            <Border.Width>
                                <MultiBinding Converter="{StaticResource ProgressToWidthConverter}">
                                    <Binding Path="ActualWidth" ElementName="barTrack"/>
                                    <Binding Path="ProgressPct"/>
                                </MultiBinding>
                            </Border.Width>
                        </Border>
                    </Border>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>